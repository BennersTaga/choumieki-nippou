{
  "name": "choumieki-nippou",
  "private": true,
  "scripts": {
    "ci:vercel": "node tools/write-clasprc.js && bash tools/deploy.sh",
    "lint": "echo skip",
    "test": "echo skip"
  },
  "devDependencies": {
    "@google/clasp": "^2.5.0"
  },
  "engines": { "node": ">=18" }
}
